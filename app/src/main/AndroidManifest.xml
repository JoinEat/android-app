<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hno2.when2eat">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.When2Eat">
        <activity android:name=".activities.CurrentGroupActivity" />
        <activity
            android:name=".activities.SelectPageActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:parentActivityName=".activities.LoginPageActivity"
            android:theme="@style/Theme.When2Eat.NoActionBar" />
        <activity android:name=".activities.LoginPageActivity" />
        <activity android:name=".activities.RegisterPageActivity" />
        <activity android:name=".activities.WelcomePageActivity" />
    </application>

</manifest>